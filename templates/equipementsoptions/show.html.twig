{% extends 'base.html.twig' %}

{% block title %}Equipementsoptions{% endblock %}

{% block body %}
    <h1>Equipementsoptions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipementsoption.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ equipementsoption.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipementsoptions_index') }}">back to list</a>

    <a href="{{ path('app_equipementsoptions_edit', {'id': equipementsoption.id}) }}">edit</a>

    {{ include('equipementsoptions/_delete_form.html.twig') }}
{% endblock %}
