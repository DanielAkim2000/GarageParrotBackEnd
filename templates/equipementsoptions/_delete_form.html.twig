<form method="post" action="{{ path('app_equipementsoptions_delete', {'id': equipementsoption.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipementsoption.id) }}">
    <button class="btn">Delete</button>
</form>
