<form method="post" action="{{ path('app_horairesouverture_delete', {'id': horairesouverture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ horairesouverture.id) }}">
    <button class="btn">Delete</button>
</form>
